# 领域 Intake 标准规范（Domain Intake Spec）

> **这是喂给 studio-ontology 的标准输入**。FDE 拿它去访谈业务方/领域专家，或直接交给对方填。
> 填完它，`ontology-map` 就能把它编成五要素 IR —— 不必先跑完整事件风暴（已有 studio-planner 产物时可跳过本表，二选一）。
>
> 每段标注：**谁来填** · **→ 编译成哪个本体要素**。能填多少填多少，填不准就留 `?`，FDE 在 ontology-map 阶段补全。
> 例子用「草业·生态修复」演示，照着替换成你的领域。

---

## 0. 元信息（FDE 填）

| 字段 | 内容 |
|---|---|
| ontology_id | grass |
| 限界上下文 | 生态修复 + 草产品交易 |
| 业务方/受访专家 | 蒙草修复工程师、营养师 |
| FDE | {你} |
| 日期 | {date} |

---

## 1. 业务概述（业务方 / PM 填）

- **这是什么业务**：给退化草地出修复方案、给饲草做品质评级与交易。
- **核心用户与诉求**：施工方要快速出合规方案；牧场要客观验草评级。
- **最想让系统"会做"的 3 件事**：① 出"一地一方"修复方案 ② 饲草快检评级 ③ 查地块能用哪些修复方法。

> → 不直接编译，但定调：哪些是"做"（Action）、哪些是"查"（Query）。

## 2. 角色 Actors（业务方 / 领域专家 填 → 角色对象 / Action 的 actor）

| 角色 | 能做什么 | 备注 |
|---|---|---|
| 施工方 | 出方案、查地块 | |
| 牧场/养殖户 | 验草评级 | |
| 监管 | 查、核证 | |

## 3. 业务对象 Entities（领域专家 / FDE 填 → **Object**）

> "名词"：有独立身份、有属性、被引用的东西。纯算出来的量**不要**写这（去第 6 段）。

| 对象 | 主键 | 关键属性（名:类型[:单位][:必填]） | 状态流转（如有） | 数据在哪 |
|---|---|---|---|---|
| 地块 Site | parcel_id | area_mu:数:亩:必填, region:文:必填, site_type:枚举 | 待勘→已勘→修复中→验收 | 政务确权+遥感 |
| 草样 ForageSample | batch_id | CP:数, NDF:数, ADF:数, RFV:数 | — | 检测中心 |
| 退化 Degradation | deg_id | level:文, type:文 | — | 分级标准 |
| 乡土名录 NativeListing | species | region:文 | — | 名录库 |

## 4. 关系 Relationships（领域专家 / FDE 填 → **Link**）

> "名词↔名词"。标注**因果性质**：根因（可终止追溯）/ 假设（继续查）/ 派生。

| 关系 | 从 | 到 | 因果性质 |
|---|---|---|---|
| 地块 遭受 退化 | Site | Degradation | 假设（continue tracing） |
| 退化 可治 修复方法 | Degradation | RestorationMethod | 派生 |
| 修复方法 用 种子包 | RestorationMethod | SeedPack | 派生 |

## 5. 业务动作 Operations（业务方 + FDE 填 → **Action**）

> "动词"：会改变世界状态、需要留痕的操作。每个动作问清：谁触发、要什么输入、产出/写什么、要不要人工审批。

| 动作 | 谁触发 | 输入 | 产出/写入 | 需人工审批？(谁) |
|---|---|---|---|---|
| 出一地一方 | 施工方 | 地块、草种、预算 | 修复方案、种子包 | 是·乡土草种合规官 |
| 快检评级 | 养殖户 | 批次、检测值 | 等级、溯源码 | 是·品质定级定价员 |

## 6. 派生指标 Metrics / KPIs（领域专家 填 → **Function**）

> 只读、算出来的量（不改状态）。给口径/公式。

| 指标 | 读取哪些对象 | 返回 | 口径/公式 |
|---|---|---|---|
| RFV分级 | ForageSample | 等级 | RFV≥151特级,≥125一级,≥103二级,≥87三级,否则等外 |
| 载畜量 | 监测、地块 | 羊单位 | 可食产草量×利用率/(单位畜日采食量×放牧天数) |

## 7. 业务规则 / 约束 Rules（领域专家 + FDE/规则工程师 填 → **Rule**）★最关键

> 系统**绝不能违反**的事。每条规则问**三件事**：① 违反要不要拦+回滚？② 只看入参能判，还是要查别的数据？③ **依据是哪条标准/方法学/谁的经验**？没依据就标 `待补依据`。

| 规则 | 拦截还是告警 | 只看入参/要查数据 | 依据（标准号/方法学/专家） | 拦什么 |
|---|---|---|---|---|
| 乡土合规 | 拦截+回滚 | 要查（地块所在地乡土名录） | GB/T 37067, DB15/T 修复规程 | 种子包含非乡土草种 |
| 预算非负 | 拦截 | 只看入参 | 通用常识 | 预算<0 |
| 霉变拦截 | 拦截+回滚 | 要查（草样霉菌毒素） | NY/T 1574 | 霉菌毒素超阈 |
| 安全播量 | 拦截 | 要查（品种合理区间） | 待补依据 | 播量超出品种区间 |

## 8. 决策点 Decision Points（领域专家 填 → 也归 **Rule**）

> 流程里的"如果…则…"。这些往往是隐藏的规则。

- 如果 盖度↓>30% 且 物种数↓ → 判重度退化（→ 一条分级规则）
- 如果 草样霉变 → 判不合格、禁入交易（→ 霉变拦截规则）

## 9. 数据来源 Data Sources（数据/IT 填 → 映射注册表）

| 对象 | 存哪（库/表） | 主键列 | 关键可查字段 |
|---|---|---|---|
| Site | postgis / geo_parcel | parcel_id | region, site_type, area_mu |
| ForageSample | clickhouse / forage_qc | batch_id | CP, NDF, ADF, RFV |

## 10. 能力问题 CQ（业务方 + FDE 填 → 验收用例）

> 系统建好后，应该能回答/办成哪些问题？至少给 1 个"查"、1 个"做"、1 个"该被拦"。

- [查] 巴彦淖尔有哪些盐碱地块？
- [做] 给 parcel_001 出一地一方，用碱茅，预算300。
- [该被拦] 给 parcel_001 用紫花苜蓿（非乡土）出方案 → 应被乡土合规拦回。

## 11. 治理要求 Governance（合规/法务 + FDE 填）

- 哪些动作必须**人工复核**（HIL）：出方案、评级、签发凭证。
- 哪些动作必须**留审计**（谁、何时、依据、为什么）：全部对外动作。
- 数据密级/出域限制：地块权属字段密级=confidential。

---

## 填写完成度自检（FDE）

- [ ] 第 3 段每个对象有主键；第 5 段动作写入的对象都在第 3 段出现
- [ ] 第 7 段每条规则标了"拦/告警 + 只看入参/要查 + 依据"；无依据的标了 `待补依据`
- [ ] 第 5 段每个改状态的动作都问了"要不要 HIL"
- [ ] 第 10 段至少 1 查 + 1 做 + 1 该被拦

> 填到这个程度，交给 `ontology-map` 即可编成五要素 IR；FDE 在 ontology-map 阶段做最后的归一、判 edge_semantics、定 declarative/function。
